<!DOCTYPE html>
<html>
<head>
  <title>Experiencely</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <% if current_user %>
    <script>
      window.CURRENT_USER_ID = <%= current_user.id %>
      // DON'T use this for forms--just for the navbar, navigation, check if logged in
      // then, can use if (window.CURRENT_USER_ID) to return a truthy or falsey value
    </script>
  <% end %>
</head>
<body>

<%= yield %>

</body>
</html>
